<header class="d-flex border-bottom align-items-center px-4 py-3" style="min-height: 4.5rem;">
    @ChildContent?.Invoke(CurrentLab)
</header>

@code {

    [CascadingParameter]
    public required LaboratoryDto CurrentLab { get; set; }

    [Parameter]
    public RenderFragment<LaboratoryDto>? ChildContent { get; set; }
}