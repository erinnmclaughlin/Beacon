@inject NavigationManager NavManager

@code {
    [Parameter, EditorRequired] public required string Url { get; set; }

    protected override void OnInitialized() => NavManager.NavigateTo("login");

}
